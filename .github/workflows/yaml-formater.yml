name: format yaml files

on:
  pull_request:
    branches: ['main']

permissions:
  contents: read

jobs:
  yamlfmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: norwd/fmtya@v1
