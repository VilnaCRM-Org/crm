export { default as {{pascalCase feature}}RepositoryImpl } from './{{feature}}-repository-impl';

export type {
  {{pascalCase feature}}Item,
  {{pascalCase feature}}Repository,
} from '../types/{{feature}}-repository';
