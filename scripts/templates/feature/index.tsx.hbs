import usePageTitle from '@/hooks/use-page-title';

import {{pascalCase feature}}Panel from './components/{{feature}}-panel';
import { use{{pascalCase feature}} } from './hooks';

export default function {{pascalCase feature}}(): JSX.Element {
  const { items, isLoading, hasError } = use{{pascalCase feature}}();

  usePageTitle('{{snakeCase feature}}.title');

  return <{{pascalCase feature}}Panel items={items} isLoading={isLoading} hasError={hasError} />;
}
