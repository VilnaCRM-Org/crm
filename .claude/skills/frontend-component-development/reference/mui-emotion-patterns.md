# MUI And Emotion Patterns

## Prefer Theme Tokens

Use MUI theme spacing, palette, and typography before hardcoded CSS values.

## Stable Styles

Move static `sx` objects or `styled` definitions outside render when they are
large or reused.

## Icons

Use `@mui/icons-material` for standard actions. Add accessible labels for
icon-only buttons.
