{"action": "create", "reason": "Episode PR #215 fixed a code review comment from cubic: `paths:` gate omitted the workflow file + toolchain, allowing silent skips on workflow changes. This is a durable pattern that applies to every GitHub Actions workflow in the repo using `paths:` filters.", "evidence": "cubic comment on PR #215: \"paths doesn't include the workflow's own build inputs → gate can silently stay unvalidated for changes to how it runs\""}
