# Testing And Quality

## Testing Workflow Changes

Update docs that mention:

- Jest environments.
- Playwright, Mockoon, or Apollo mock behavior.
- Visual snapshot update rules.
- Lighthouse, K6, Memlab, or Stryker commands.

## Quality Workflow Changes

Update docs that mention:

- `make format`
- `make lint`
- `make lint-metrics`
- Qlty or Prettier behavior

Finish with `make format`, then `make lint-md`, then `make lint` for code changes.
