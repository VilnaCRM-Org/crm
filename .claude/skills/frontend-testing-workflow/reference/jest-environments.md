# Jest Environments

## Client

Use for React components, hooks, DOM behavior, and browser utilities:

```bash
make test-unit-client
```

## Server

Use for Apollo local mock server tests:

```bash
make test-unit-server
```

## Integration

Use when behavior crosses more than one frontend boundary:

```bash
make test-integration
```
