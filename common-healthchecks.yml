x-healthcheck-default: &healthcheck-default
  test: ["CMD", "true"]
  interval: 10s
  timeout: 5s
  retries: 3
  start_period: 30s

services:
  apollo:
    healthcheck: *healthcheck-default
  mockoon:
    healthcheck: *healthcheck-default
